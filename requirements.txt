# AI Flight Planner - Backend Dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0

# HTTP client (async LLM calls)
httpx>=0.27.0,<1.0.0

# Graph search
networkx>=3.2,<4.0

# Data validation
pydantic>=2.5.0,<3.0.0

# Database
# SQLite3 is part of Python stdlib — no extra package needed.
# Optional: aiosqlite for async DB access (not used currently).
