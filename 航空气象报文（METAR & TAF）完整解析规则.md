# 航空气象报文（METAR & TAF）完整解析规则

本文档详细解析了全球通用的航空气象报文编码规则，包含实况报告（METAR）与终端预报（TAF）的解码指南。

---

##  第一部分：METAR（航空例行天气报告）

METAR（Meteorological Aerodrome Report）反映的是机场在特定时间的**实时天气状况**。

### 1. 报文基本结构
报文通常由空格分隔的多个数据组组成，基本顺序如下：
`报告类型 机场代码 观测时间 风 能见度 跑道视程 天气现象 云 温度/露点 气压 趋势`

### 2. 详细解码规则

#### 2.1 报告类型与位置时间
- **METAR**：每小时或每半小时发布的例行天气报告。
- **SPECI**：天气出现显著变化时（如能见度骤降、突发雷暴）发布的特殊天气报告。
- **机场代码**：4个字母的ICAO代码（如 `ZSSS` 上海虹桥，`RJAA` 东京成田，`KJFK` 纽约肯尼迪）。
- **观测时间**：6位数字 + `Z`。前两位是日期，后四位是时间（UTC世界协调时）。
  - 例：`280130Z` 代表本月28日 01:30 (UTC)。

#### 2.2 风（Wind）
- **常规格式**：`dddffGfmfmKT` 或 `dddffGfmfmMPS`
  - `ddd`：风向（3位数，真北方位）。
  - `ff`：平均风速。
  - `Gfmfm`：阵风（Gust），后接阵风最大风速。
  - `KT`：单位为“节”（国际通用）；`MPS`：单位为“米/秒”（中国国内常用）。
- **特殊情况**：
  - `00000KT`：静风（无风）。
  - `VRB`：风向多变（Variable），如 `VRB03KT`。
  - `dddVddd`：风向摆动范围，如 `140V200`（风向在140度至200度之间变化）。

#### 2.3 能见度（Visibility）
- **常规数值**：
  - 公制：直接显示米数，如 `3000`（3000米）。
  - 英制：显示英里数，如 `3/8SM`（3/8英里），`10SM`（10英里）。
- **特殊标识**：
  - `9999`：能见度 ≥ 10公里（良好）。
  - `CAVOK`：天气良好（Ceiling And Visibility OK）。条件：能见度≥10公里，1500米（或5000英尺）以下无云，且无重要天气现象。

#### 2.4 跑道视程（RVR）
- 格式：`R` + 跑道编号 + `/` + 视程数值 + (趋势)。
- 例：`R32L/1000FT`（32号左跑道视程1000英尺）。
- 趋势标识：`U`（上升）、`D`（下降）、`N`（无变化）。
- 极值标识：`M`（小于，如 `M0600`）、`P`（大于，如 `P6000`）。

#### 2.5 天气现象（Weather Phenomena）
由“强度 + 描述符 + 降水/遮蔽类型”组合而成：

| 强度 | 描述符 | 降水/遮蔽类型 |
| :--- | :--- | :--- |
| `-` 轻度 | `MI` 浅 | `RA` 雨 |
| (无) 中度 | `PR` 部分 | `SN` 雪 |
| `+` 重度 | `BC` 碎片 | `DZ` 毛毛雨 |
| `VC` 附近 | `DR` 低吹 | `SG` 雪粒 |
| | `BL` 高吹 | `PL` 冰粒 |
| | `SH` 阵性 | `GR` 冰雹 |
| | `TS` 雷暴 | `GS` 小冰雹/霰 |
| | `FZ` 冻结 | `BR` 轻雾 (能见度≥1000m) |
| | | `FG` 雾 (能见度<1000m) |
| | | `HZ` 霾 |
| | | `SA` 扬沙 |

- 例：`-SHRA`（小阵雨）、`+TSRA`（强雷阵雨）、`FZRA`（冻雨）。

#### 2.6 云（Clouds）
格式：`量 高 (型)`
- **覆盖量**：
  - `SKC` / `CLR`：晴空 / 无云。
  - `FEW`：少云（1-2/8云量）。
  - `SCT`：疏云（3-4/8云量）。
  - `BKN`：多云（5-7/8云量，通常视为云底高）。
  - `OVC`：阴天（8/8云量，满天云）。
- **高度**：3位数字，代表云底高度，单位是**百英尺**。
  - 例：`BKN020` 代表 2000英尺高度有多云。
- **特殊云型**：
  - `CB`：积雨云（伴随雷暴，极度危险）。
  - `TCU`：浓积云。

#### 2.7 温度与露点、气压
- **温度/露点**：`温度/露点`（摄氏度）。`M` 代表负数。
  - 例：`24/M02`（气温24℃，露点-2℃）。
- **气压**：
  - `Q` 开头：百帕（hPa），如 `Q1012`（国际/中国常用）。
  - `A` 开头：英寸汞柱（inHg），如 `A2992`（美国常用）。

#### 2.8 趋势与备注
- **NOSIG**：未来2小时内无显著变化。
- **BECMG**：未来2小时内天气将逐渐转变。
- **TEMPO**：未来2小时内会有短暂的天气波动。
- **RMK**：备注信息的开始（如自动站传感器类型、海平面气压等）。
- **=**：报文结束符（电报时代遗留规范）。

---

##  第二部分：TAF（航站天气预报）

TAF（Terminal Aerodrome Forecast）是对未来 **24至30小时** 机场天气的预测。

### 1. 报文基本结构
`TAF 机场代码 发布时间 有效期 预报气象要素组`

### 2. 核心变化标识（TAF的灵魂）
TAF通过以下缩写来分段描述不同时间段的天气变化：

- **FM (From)**：天气的**快速转变**。
  - 例：`FM281200` 代表从 28日 12:00 (UTC) 开始，天气将突然变为后面描述的状态。
- **BECMG (Becoming)**：天气的**逐渐转变**。
  - 例：`BECMG 2812/2815` 代表在 28日 12:00 至 15:00 期间，天气会慢慢过渡到新状态。
- **TEMPO (Temporary)**：天气的**短暂波动**。
  - 例：`TEMPO 2814/2818` 代表在该时段内，天气会暂时变差，但不会持续很久（通常不超过该时段的一半）。
- **PROB (Probability)**：天气现象发生的**概率**。
  - 例：`PROB30` 代表有 30% 的概率出现后面描述的恶劣天气。

### 3. 气温与特殊预报
- **TX (最高温)**：`TX34/2806Z`（28日06Z预计最高温34℃）。
- **TN (最低温)**：`TN29/2822Z`（28日22Z预计最低温29℃）。
- **NSW (No Significant Weather)**：预期没有重要的天气现象。
- **NSC (No Significant Cloud)**：没有对飞行有重要影响的云层。

---

> ** 提示**：在实际应用中，METAR用于飞行员起降前的实时条件确认，而TAF用于航班提前规划和燃油计算。遇到 `CB`（积雨云）、`FG`（雾）或 `TS`（雷暴）等代码时，需格外警惕飞行安全风险。